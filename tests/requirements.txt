# Test-only dependencies for the Orcha API state-machine suite (Orcha#22).
# The app's own deps (fastapi, pydantic) come from orcha-cli/.../portal/requirements.txt.
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27
psycopg[binary]>=3.1
fastapi>=0.110
pydantic>=2
python-multipart>=0.0.9
qrcode>=8
# QR scannability gate (test-only, never shipped in the portal image): rasterize the
# styled pairing SVG and decode it back — tests/test_pairing_api.py round-trip.
pillow>=10
zxing-cpp>=2.2
# Push-relay unit tests (tests/test_push_relay.py): the relay's APNs JWT signing
# deps (deploy/push-relay/requirements.txt) — never shipped in the portal image.
pyjwt>=2.8
cryptography>=42
